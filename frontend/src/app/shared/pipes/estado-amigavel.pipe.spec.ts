import { EstadoAmigavelPipe } from './estado-amigavel.pipe';

describe('EstadoAmigavelPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoAmigavelPipe();
    expect(pipe).toBeTruthy();
  });
});
