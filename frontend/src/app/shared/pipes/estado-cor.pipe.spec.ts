import { EstadoCorPipe } from './estado-cor.pipe';

describe('EstadoCorPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCorPipe();
    expect(pipe).toBeTruthy();
  });
});
