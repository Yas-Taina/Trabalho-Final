import { EnderecoCompletoPipe } from "./endereco-completo.pipe";


describe('EnderecoCompletoPipe', () => {
  it('create an instance', () => {
    const pipe = new EnderecoCompletoPipe();
    expect(pipe).toBeTruthy();
  });
});
